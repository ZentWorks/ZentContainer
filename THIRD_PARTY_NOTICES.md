# Third-Party Notices

The ZentContainer Go source uses only the Go standard library.

The distributed Docker image installs Alpine Linux packages including:

- Docker CLI
- Docker Compose plugin
- SQLite CLI
- CA certificates
- timezone data

These components remain governed by their respective upstream licenses. Their license notices are not relicensed by the ZentContainer MIT license.
